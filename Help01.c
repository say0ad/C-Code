#include<stdio.h>
int main()
{
    int ak;
    printf("Enter the Number:");
    scanf("%d",&ak);

    if(ak>0)
    {
        printf("The Number is positive.");
    }
    else
    {
        printf("The number is Negetive.");
    }
    return 0;
}
